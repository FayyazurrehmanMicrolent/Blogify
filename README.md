
  # Set User Display Name

  This is a code bundle for Set User Display Name. The original project is available at https://www.figma.com/design/Bt8pifXo8M31Mmg0DlPvsS/Set-User-Display-Name.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  